#include <stdio.h>
#include <stdlib.h>
#include"class.h"
int main(){
    int n,ch = 0,ar[100],ln = 0,pos = 0,start,end,v;

    t :  printf("How many values to be inserted (i-50) : ");
    scanf("%d", &n);
    if(n > 0 && n <= 50){
            printf("Insert value\n");
            OneDIntArrayInput(ar,n);
    }
    else{
        printf("\nWrong Number of values!!");
        goto t;
    }
    while (1) {
        printf("\n      Select Operation \n");
        printf("press 0 : Exit \n");
        printf("press 1 : Insert at last Position\n");
        printf("press 2 : Insert at specific position \n");
        printf("press 3 : Display array\n");
        printf("press 4 : Delete from specific position \n");
        printf("press 5 : for liner Search \n");
        printf("press 6 : Bubble sort\n");
        printf("press 7 : Binary Search \n");
        printf("Enter your choice: ");
        scanf("%d", &ch);
        switch(ch){
            case 0:
                exit(0);
            case 1:
                printf("          Your choice : 1 \n");
                printf("     Insert a last operation\n");
                int v;
                scanf("%d",&v);
                ln = insert_last(ar,n,v);
                printf("\n Inserted Successfully\n");
                break;
            case 2:
                printf("          Your choice : 2 \n");
                printf("     Insertion at specific position\n");
                printf("\nEnter your position : ");
                scanf("%d",&pos);

                if(pos >= 0 && pos <= ln){
                    ln = insert_specific(ar,ln,pos);
                }
                else{
                    printf("Invalid position!\n");
                }
                break;

            case 3:
                printf("      Your choice : 3 \n");
                printf("Display array :- ");
                if(n > 0){
                    Displayarray(ar,ln);
                }
                else{
                    printf("Invalid position!\n");
                }
                break;
            case 4:
                break;
            case 5:
                printf("     Your choice : 5\n");
                    printf("Enter value for searching : ");
                if(n > 0){
                    scanf("%d",&v);
                    linear_scarch(ar,n,v);
                }
                else{
                    printf("Array is Empty\n");
                }
                break;
            case 6:
                printf("\nYour choice is bubble sort  -- ");
                scanf("%d",&v);
                if(n > 0){
                    bubblesort(n,ln);
                    printf("\nSuccesfully Sorted");
                }
                else{
                    printf("\nThe array is empty");
                }
                break;
            case 7:
                printf("\nYour choice Binary Search");
                scanf("%d",&v);
                if(n > 0){
                    printf("\n");
                    binarySearch(ln,n,v);
                    
                }
                else{
                    printf("\nThe array is empty");
                }
                break;
            case 9:

            default:
                printf("Invalid choice!\n");
        }
    }
    return 0;
}
