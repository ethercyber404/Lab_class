#include <stdio.h>
#include <stdlib.h>
#include"class.h"
int main(){
    int n,ch,ar[100],a,pos = 0,start,end,v;
    t :  printf("How many values to be inserted (1-50) : ");
    scanf("%d",&n);
    if(n > 0 && n <= 50){
            printf("Insert value\n");
            OneDIntArrayInput(ar,n);
    }
    else{
        printf("\nWrong Number of values!!");
        goto t;
    }
    while(1)
    {
        printf("\nSelect Operation \n");
        printf("press 0 : Exit \n");
        printf("press 1 : Insert at last Position\n");
        printf("press 2 : Insert at specific position \n");
        printf("press 3 : Display array\n");
        printf("press 4 : Delete from specific position \n");
        printf("press 5 : for liner Search \n");
        printf("press 6 : Bubble sort\n");
        printf("press 7 : Binary Search \n");
        printf("Enter your choice: ");
        scanf("%d",&ch);
        switch(ch){
            case 0:
                exit(0);
            case 1:
                printf("Your choice : Insert at last Position \n");
                int v;
                n = insert_last(ar,n,v);
                break;
            case 2:
                printf("Your choice : Insert at specific position \n");
                printf("\nEnter your position : ");
                scanf("%d",&pos);

                if(pos >= 0 && pos <= a){
                    a = insert_specific(ar,a,pos);
                }
                else{
                    printf("Invalid position!\n");
                }
                break;
            case 3:
                printf("Your choice : Display array\n");
                printf("Display array :- ");
                if(n > 0){
                    Displayarray(ar,n); 
                } 
                else{
                    printf("Array is empty!\n");
                }
                break;
            case 4:
                break;
            case 5:
                printf("Your choice : for liner Search\n");
                    printf("Enter value for searching : ");
                if(n > 0){
                    scanf("%d",&v);
                    linear_scarch(ar,n,v);
                }
                else{
                    printf("Array is Empty\n");
                }
                break;
            case 6:
                printf("\nYour choice is bubble sort  -- ");
                if(n > 0){
                    bubblesort(ar,n);
                    printf("\nSuccesfully Sorted");
                }
                else{
                    printf("\nThe array is empty");
                }
                break;
            case 7:
                bubblesort(ar,n);

                printf("\nYour choice Binary Search");
                printf("\nEnter the index to be search : ");
                scanf("%d",&v);
                if(n > 0){
                    start = 0; end = n-1;
                    printf("\n");
                    binarySearch(ar,start,end,v);
                }
                else{
                    printf("\nThe array is empty");
                }
                break;
            default:
                printf("Invalid choice!\n");
        }
    }
    return 0;
}
